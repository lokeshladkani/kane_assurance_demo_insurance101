# CI orchestration scripts are stdlib-only (json, subprocess, pathlib).
# This file exists so `pip install -r requirements.txt` in the workflow always succeeds
# and gives a place to pin future dependencies.
